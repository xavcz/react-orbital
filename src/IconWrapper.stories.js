import { storiesOf } from '@kadira/storybook';
import React from 'react';
import IconWrapper from './IconWrapper';

storiesOf('IconWrapper', module).add('default', () => <IconWrapper>🚀</IconWrapper>);
